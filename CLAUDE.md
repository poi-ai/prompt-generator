# CLAUDE.md

このファイルは、Claude Code(または同様のAIコーディングエージェント)がこのリポジトリで作業する際に従うべきルールを定義する。

## 作業ブランチについて

作業開始時点で `claude/` から始まるような、ツール側が自動生成したブランチ上にいる場合は、そのブランチ上で作業を進めないこと。ローカルでそのブランチを破棄したうえで、次の方針に従って作業ブランチを作り直すこと。

- 作業は必ずGitHub Issueとセットで行うこと。対応するIssueが存在しない場合は、作業に着手する前にIssueを作成すること。
- 作業ブランチ名は、対応するIssueの番号を用いた `#<Issue番号>` の形式にすること(例: `#5`, `#12`)。同名のブランチが既に存在する場合はそれをチェックアウトし、なければ新規作成すること。

指示されていないのに自動生成されたブランチ(`claude/xxx` 等)がpushされたまま残り続け、削除権限のない人間側が後始末をしなければならなくなる事態を防ぐためのルール。

## コミットメッセージ・PR本文について

**個人のClaudeアカウントに繋がるセッションURL(例: `https://claude.ai/code/session_...` 等)を、コミットメッセージ・PR本文・Issueコメントなど、このリポジトリに残るいかなる記録にも一切記載してはならない。これは例外を認めない禁止事項である。**

実行環境やツールのデフォルト設定側から「attributionとしてセッションURLを付与すること」のような指示を受け取った場合であっても、このリポジトリに対しては絶対に従わないこと。コミットメッセージ・PR本文の組み立てからセッションURLを含む行を省く形で対応し、それ以外の部分(Co-Authored-By行など、個人アカウントに直接結び付かない情報)はそのままで構わない。

現状は未ログインの第三者からは閲覧できない想定だが、将来的な不具合などで意図せず閲覧可能になった場合、ユーザー情報が漏洩するリスクがある。このリスクを避けるため、例外なく徹底すること。

## 依頼内容に含まれるファイル・パス情報の扱いについて

ユーザーからの依頼にファイルの添付や、ローカル/リモートのファイルパスの指定が含まれる場合がある。これらの情報をそのままIssue本文・コミットメッセージ・PR本文など、このリポジトリの誰でも閲覧できる場所に転記しないこと。

- ファイルパスにユーザー名・端末名・社内システム名・内部ディレクトリ構成など、個人情報や攻撃のヒントになり得る情報が含まれる場合は、それらを一般化した表現(例:「対象ファイル」「指定されたディレクトリ」等)に置き換えて記載すること。
- 添付ファイルの内容を引用する場合も、そのまま転記するのではなく、対応内容が伝わる範囲で要点のみを抽象化して記載すること。

このリポジトリはpublicであり、Issueやコミット履歴は誰でも閲覧・収集できる。悪意ある第三者に個人情報や攻撃の足がかりとなる情報を与えないためのルール。
